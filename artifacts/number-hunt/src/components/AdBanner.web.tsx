/**
 * Web stub for AdBanner — renders nothing.
 *
 * Metro picks this file over AdBanner.tsx when bundling for web, so the
 * Replit preview and browser builds never try to load the native AdMob
 * SDK (which would break the bundle).
 */

import React from "react";

export function AdBanner(_props: { bottomInset?: number }): React.ReactElement | null {
  return null;
}
